# Release v1.1.4

Date: 2026-05-08

## Highlights

- Starts the external SikuRoBERTa punctuation Docker service from the main start scripts when configured.
- Defaults the main app to the local external punctuation URL and documents the upstream model attribution.
- Starts and detects the OpenAI OAuth proxy from `start_server.bat`.
- Improves annotation and citation tab block selection fallbacks across TextBlock/LayoutBlock/default-block workflows.
- Fixes citation creation from original-text drag selection and citation context fallback to the stored source-text snapshot.

## Verification

- `uv run python -m pytest -q tests/test_citation_mark.py -p no:cacheprovider`
- `uv run python -m pytest -q tests/test_llm_integration.py -p no:cacheprovider`
- `node --check src/app/static/js/annotation-editor.js`
- `node --check src/app/static/js/citation-editor.js`
